# shellcheck shell=bash
# Shellclaw configuration
# This file is sourced directly by config_init — use only variable assignments.
# Lines starting with # are comments. No export statements needed.
# shellcheck disable=SC2034  # variables are used by the sourcing script

# LLM model identifier (passed to llm CLI via -m flag)
SHELLCLAW_MODEL="claude-sonnet-4-5-20250929"

# Default agent to use when none specified
SHELLCLAW_DEFAULT_AGENT="default"

# Tool-calling backend: bash | llm | elixir
# Selects which tool-loop implementation handles tool calls.
# Not used in v2.0 (chat-only scope).
SHELLCLAW_TOOL_BACKEND="bash"

# LLM backend: llm | stub
# Use "stub" for testing without API calls or llm CLI dependency.
SHELLCLAW_LLM_BACKEND="llm"

# Directory containing self-describing tool executables
SHELLCLAW_TOOLS_DIR="tools"
